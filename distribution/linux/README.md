# Linux distribution target

Linux packaging is reserved here so it does not become mixed into the Windows
or macOS release systems. The game will use the shared desktop window/input and
audio layers with native Vulkan. Package format, supported distributions,
architectures, and updater behavior will be selected when Linux work begins.

No Linux release asset should be published until its runtime dependency and
compatibility tests exist.

