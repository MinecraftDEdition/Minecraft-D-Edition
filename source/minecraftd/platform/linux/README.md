# Linux platform glue

This directory is reserved for Linux-only application paths, dialogs, and
runtime integration. Window/input, Vulkan surface creation, and audio should be
shared with the desktop layer whenever possible.

