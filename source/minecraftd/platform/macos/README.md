# macOS platform glue

This directory will contain only behavior that cannot live in the shared SDL3
desktop layer, such as app-bundle paths, macOS dialogs, MoltenVK loading details,
and platform release integration. Game rules and network serialization do not
belong here.

