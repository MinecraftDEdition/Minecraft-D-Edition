# Shared desktop platform layer

This directory is reserved for the SDL3 window/input implementation and the
portable desktop audio bridge. Windows will adopt these implementations first
where practical so behavior can be verified before the first macOS build.

